import { beforeEach, describe, it } from 'vitest'
import { ProductTypeormRepository } from '../ProductTypeormRepository'

describe('ProductTypeormRepository integration tests', () => {
  let productTypeormRepository: ProductTypeormRepository

  beforeEach(() => {
    productTypeormRepository = new ProductTypeormRepository()
  })

  describe('method', () => {
    it('testng', async () => {})
  })
})
